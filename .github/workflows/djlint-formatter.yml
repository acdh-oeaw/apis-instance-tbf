# SPDX-FileCopyrightText: 2024 K Kollmann
# SPDX-License-Identifier: MIT

name: Run djLint formatter

on:
  # runs when activity type is one of: opened, synchronize, reopened
  pull_request:
    paths:
      - "**.html"
  workflow_dispatch:

jobs:
  djlint-formatter:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Install the latest version of uv
        uses: astral-sh/setup-uv@v7

      - name: Install only dependencies for formatting/linting
        run: uv sync --only-group lint

      - name: Run djlint with --check
        run: uv run --no-sync djlint . --check
